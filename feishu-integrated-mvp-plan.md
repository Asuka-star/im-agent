# 飞书接入版 MVP 方案

## 目标

这版方案用于替代原来的“自建聊天页面主导”思路，改成：

- 以 `飞书 IM` 作为真实入口
- 以 `飞书消息接收 + 自动回复` 作为核心闭环
- 以 `多维表格` 作为可选的结构化任务看板

目标是在 `2026-04-17 23:59` 报名前，做出一个更贴合比赛课题的可演示 MVP。

## 项目定位

项目暂定名：

- `飞书群聊协同智能助手`

一句话定义：

- 团队成员在飞书群聊中讨论需求、会议安排或项目推进时，助手自动接收消息，提取决策与待办，给出负责人和截止时间建议，识别风险项，并将协作结果回复到群聊中；如有需要，再同步写入飞书多维表格。

## 为什么这版更贴合课题二

对应课题：

- `飞书 AI 产品创新赛道 - 基于 IM 的办公协同智能助手`

这版比原方案更贴题，因为它满足了这几个关键点：

- `IM 是真实入口`
  - 不是单独做一个仿聊天网页，而是直接在飞书消息流里发生交互
- `办公协同是核心场景`
  - 输入来自群聊讨论、会议纪要、需求推进，而不是单纯分析一段文本
- `协同结果直接回到会话`
  - 输出是群聊里的任务整理、风险提醒、跟进建议
- `可持续跟进`
  - 后续成员继续在群里提问，助手可以结合上下文继续回答

## 志愿建议

建议仍然填这组：

1. `第一志愿：飞书 AI 产品创新赛道 - 基于 IM 的办公协同智能助手`
2. `第二志愿：飞书 AI 产品创新赛道 - Multi-Agent Network - 多维表格上的多智能体虚拟组织`
3. `第三志愿：飞书 OpenClaw 赛道 - 企业级长程协作 Memory 系统`

原因：

- 第一志愿和当前方案最强匹配
- 第二志愿可以由“多个 Agent 围绕任务状态协作”来支撑
- 第三志愿可以由“历史上下文记忆、持续跟进”来支撑

## 产品最小闭环

### 必须实现

1. 用户在飞书群聊里发送一段需求讨论或会议内容
2. 应用收到飞书消息事件
3. 后端调用 LLM 完成结构化分析
4. 助手把结果回复到飞书群聊中
5. 用户继续追问，助手结合历史上下文继续回答

### 可选增强

6. 把任务同步到飞书多维表格

## 不建议的接入方式

这次不建议把 `自定义 Webhook 机器人` 作为核心实现方式。

原因：

- 它更适合单向消息推送
- 不适合作为“读消息 -> 理解上下文 -> 持续协同”的主入口

这次更推荐的方案是：

- `自建应用`
- 开启 `机器人能力`
- 使用 `接收消息` 事件
- 使用 `发送消息` 接口回复
- 如有余力，再接 `多维表格新增记录`

## 推荐接入能力

### 核心能力

- `接收消息事件`
  - 用来接收单聊或群聊中的消息
- `发送消息接口`
  - 用来把结果回复给用户或群组

### 推荐增强

- `多维表格新增记录`
  - 用来把待办同步成任务看板

### 接收事件方式

优先尝试：

- `长连接接收事件`

这样做的好处是：

- 前期不需要先折腾公网回调地址
- 更适合这几天快速做 MVP

## MVP 范围

### 必须做

- 飞书应用创建与基础配置
- 机器人接收群聊消息
- Python 后端处理消息
- 自动回复结构化协同结果
- SQLite 保存会话历史和任务状态
- 支持 1 到 2 轮持续追问

### 能做更好，但不是必须

- 同步到飞书多维表格
- 更细的任务状态管理
- 更漂亮的 Web 管理面板

### 暂时不做

- 登录系统
- 多用户权限体系
- 复杂审批流
- 真正企业级部署

## 项目演示方式

Demo 不再以“打开网页粘贴文本”为主，而是改成：

1. 打开一个飞书群
2. 在群里发送会议纪要或需求讨论文本
3. 助手自动回复：
   - 项目目标摘要
   - 待办列表
   - 负责人建议
   - 截止时间建议
   - 风险提醒
4. 再发送一条追问：
   - “现在最大风险是什么？”
   - “谁的任务还没明确？”
   - “帮我生成一条群里的跟进消息”
5. 助手基于历史上下文继续回复
6. 如果做了多维表格同步，再展示表里生成的任务记录

## 多 Agent 设计

即使接入飞书，内部仍然保持多 Agent 结构，但不需要在第一版做得太复杂。

### `PlannerAgent`

- 理解本轮消息内容
- 提取会议目标、讨论结论、待办项

### `CoordinatorAgent`

- 为任务补充负责人建议
- 为任务补充优先级和截止时间建议

### `ReviewerAgent`

- 检查任务是否缺失负责人
- 检查截止时间是否缺失
- 检查是否有冲突、依赖不清、风险过高的事项

### `MemoryAgent`

- 保存会话摘要
- 保存历史任务状态
- 为 follow-up 提供上下文

## 推荐输出格式

回复到飞书群里的内容建议统一成固定模板，便于稳定演示：

```text
【本轮协同总结】
1. 本次讨论目标：
2. 已识别待办：
3. 建议负责人：
4. 建议截止时间：
5. 当前风险：
6. 下一步建议：
```

如果做多维表格同步，则每条任务可包含这些字段：

- 任务名
- 负责人
- 优先级
- 截止时间
- 状态
- 风险备注
- 来源消息时间

## 技术路线

后端建议：

- `Python`
- `FastAPI`
- `SQLite`

前端建议：

- 第一版不强依赖前端
- 如需补一个展示页，可后续再用 AI 生成

这样做的好处是：

- 时间更集中在飞书接入和核心协同逻辑
- 更容易把 Demo 做成“真实 IM 场景”

## 3 天开发计划

## Day 1：飞书接入打通

目标：

- 跑通“飞书发消息 -> 应用接收 -> 应用回消息”

### Day 1 任务

1. 创建飞书自建应用

- 开启机器人能力
- 获取应用鉴权所需配置

2. 配置消息能力

- 打开 `接收消息` 所需事件能力
- 打开 `发送消息` 所需接口权限

3. 搭建 Python 后端骨架

- `FastAPI`
- 基础日志
- 环境变量管理

4. 接入飞书事件接收

- 优先尝试 `长连接接收事件`
- 若当前环境不方便，再考虑回调模式

5. 打通回复链路

- 收到一条群消息后
- 先不做复杂分析
- 直接回一条固定文本，确认链路通

### Day 1 完成标准

- 群里发消息，应用能收到
- 应用能自动回复

## Day 2：接入 AI 协同能力

目标：

- 把“自动回复”升级成“协同助手回复”

### Day 2 任务

1. 定义统一输入输出结构

输入：

- 会话 ID
- 消息 ID
- 发送者
- 原始消息文本

输出：

- 摘要
- 任务列表
- 风险列表
- 下一步建议
- Agent trace

2. 实现 4 个最简 Agent

- `PlannerAgent`
- `CoordinatorAgent`
- `ReviewerAgent`
- `MemoryAgent`

3. 编写 orchestrator

- `Planner -> Coordinator -> Reviewer -> Memory`

4. 接入 LLM

- 统一从原始消息文本中提取结构化结果

5. 把结果回复到飞书群聊

- 用统一模板输出
- 保证稳定可读

6. 落库

- 保存会话
- 保存消息
- 保存任务
- 保存摘要与风险

### Day 2 完成标准

- 群里发一段会议纪要，应用能自动回协同结果
- 数据能保存到本地数据库

## Day 3：补记忆与多维表格

目标：

- 让项目具备“持续协同”而不是一次性总结的感觉

### Day 3 任务

1. 做 follow-up

支持用户在群里继续问：

- “当前最大风险是什么？”
- “还有哪些任务没有负责人？”
- “帮我写一条跟进消息”

2. 接入 Memory

- 拉取当前会话最近几轮摘要
- 合并历史任务状态
- 再交给模型生成回答

3. 可选接入多维表格

- 将任务写入飞书多维表格
- 至少新增一条记录成功

4. 固定一条 Demo 脚本

- 群里输入会议纪要
- 助手回复任务和风险
- 再追问一次
- 展示记忆效果
- 如果有多维表格，再展示表中任务记录

5. 准备报名文案

- 项目名称
- 项目简介
- 核心功能
- 创新点

### Day 3 完成标准

- 有可演示的飞书群聊 Demo
- 至少支持一轮上下文跟进
- 如果有余力，完成多维表格同步

## 推荐目录结构

```text
backend/
  app/
    main.py
    feishu/
      auth.py
      event_handler.py
      message_api.py
      bitable_api.py
    agents/
      planner.py
      coordinator.py
      reviewer.py
      memory.py
      orchestrator.py
    db/
      database.py
      models.py
    services/
      llm.py
      memory_service.py
    schemas/
      feishu_event.py
      task.py
      session.py
  requirements.txt
```

## 当前最优先事项

真正的优先级应该是：

1. 先打通飞书消息接收和回复
2. 再做 AI 结构化分析
3. 再做 Memory
4. 最后再做多维表格

不要反过来做。

## 官方来源

以下链接已于 `2026-04-14` 进行核对，可作为本方案的官方依据：

1. `发送消息 - 服务端 API`
   - 链接：https://open.feishu.cn/document/server-docs/im-v1/message/create
   - 用途：确认应用可以向指定用户或群聊发送消息，是回复协同结果的核心接口。

2. `接收消息 - 服务端 API`
   - 链接：https://open.feishu.cn/document/server-docs/im-v1/message/events/receive
   - 用途：确认应用可以接收单聊或群聊中的消息事件，是 IM 助手的核心入口。

3. `事件订阅配置 / 使用长连接接收事件`
   - 链接：https://open.feishu.cn/document/server-docs/event-subscription-guide/event-subscription-configure-/request-url-configuration-case
   - 用途：确认事件订阅支持开发者服务器和长连接方式；本 MVP 优先考虑长连接以降低前期接入复杂度。

4. `新增记录 - 多维表格服务端 API`
   - 链接：https://open.feishu.cn/document/server-docs/docs/bitable-v1/app-table-record/create
   - 用途：确认可以将任务结构化写入飞书多维表格，用于展示协同看板。

5. `自定义机器人使用指南`
   - 链接：https://open.feishu.cn/document/client-docs/bot-v3/add-custom-bot
   - 用途：用于和自建应用方案做区分。它适合消息推送，但不建议作为本项目的主入口方案。

## 当前结论

这版 MVP 的核心不再是“做一个仿聊天网页”，而是：

- 真正进入飞书 IM
- 在真实群聊中工作
- 围绕办公协同输出结果
- 用 Memory 做持续跟进

如果我们按这版推进，项目会比之前明显更符合：

- `飞书 AI 产品创新赛道 - 基于 IM 的办公协同智能助手`
